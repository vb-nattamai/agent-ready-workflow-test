{
  "_comment": "<!-- Generated by AgentReady (LLM-first mode) | Dynamic section: do not edit manually. Static section in agent-context.json: safe to edit. -->",
  "mcpServers": {
    "order-service-tools": {
      "command": "python",
      "args": [
        "tools/example_tool.py"
      ],
      "env": {
        "ANTHROPIC_API_KEY": "${ANTHROPIC_API_KEY}",
        "INSTALL_TOKEN": "${INSTALL_TOKEN}",
        "GH_TOKEN": "${GH_TOKEN}",
        "PR_NUMBER": "${PR_NUMBER}",
        "PYTHONPATH": "${PYTHONPATH}"
      }
    }
  }
}